# Node modules folder: dependencies installed via npm/yarn.
node_modules/

# SvelteKit generated folder: contains build and development artifacts.
.svelte-kit/

# Vite build output folder.
build/

# Environment variable files: keep these local and secure.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Log files: these are generated during development.
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Lock files:
# If you list package-lock.json or yarn.lock here, they will be ignored by default.
# However, if you manually add them with `git add`, they will be tracked in your repo.
package-lock.json
yarn.lock

# macOS specific files.
.DS_Store